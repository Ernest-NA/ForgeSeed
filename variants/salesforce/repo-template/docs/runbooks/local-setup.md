# Local Setup

## Checklist
1. Install `sf` CLI
2. Clone the repository
3. Read `AGENTS.md`
4. Authenticate to the target org or Dev Hub
5. Review `sfdx-project.json` and `manifest/package.xml`
6. Retrieve or deploy metadata as needed
7. Run validation commands
